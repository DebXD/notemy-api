Search Notes
---

tags:
  - "Notes"
parameters:
  - in: header
    name : "Authorization"
    description : "Bearer <TOKEN>"
    required : true
  - in: query
    name : query
    description : "Search query"
    required : true
    schema:
      type: integer

    
responses:
  200:
    description : "Sucessfully fetched notes"

  400:
    description: "improper input"

  401:
    description : "wrong Token"