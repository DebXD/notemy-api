Search Notes
---

tags:
  - "Notes"
parameters:
  - in: header
    name : "Authorization"
    required : true
  - in: query
    name : query
    required : true

    
responses:
  200:
    description : "When a user tries to get notes"

  400:
    description: "Fails to give proper input"

  401:
    description : "Provides a wrong Token"